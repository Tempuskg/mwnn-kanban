import type { ProFeatureRegistrar } from './types';

export const registerProFeatures: ProFeatureRegistrar = (context) => {
  context.log('MWNN Kanban Pro local development stub loaded; no Pro features are registered.');
};
